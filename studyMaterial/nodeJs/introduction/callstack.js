const number = 610 // allocate memory for number
const string = 'some' // allocate memory for a string
let human = { // allocate memory for an object and its values
    first: 'Andre',
    Last: 'Nun'
}

human = 5

function calculate(){
    const sumTotal = 4 + 5
    return sumTotal
}

//callstack adds calculate on top of the stack and then it clears it